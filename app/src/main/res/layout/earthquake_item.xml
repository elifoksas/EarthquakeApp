<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:layout_marginStart="8dp"
    android:layout_marginTop="3dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="3dp"
    android:background="@drawable/earthquake_item_background"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:paddingStart="10dp"
    android:paddingEnd="10dp">

    <TextView
        android:id="@+id/intensityTV"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/earthquake_detail_magnitude_circle"
        android:gravity="center"
        android:includeFontPadding="false"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="4.2" />

    <TextView
        android:id="@+id/countryTV"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/earthquake_text_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/minutesPassedTV"
        app:layout_constraintEnd_toStartOf="@id/metadataDivider"
        app:layout_constraintStart_toEndOf="@id/intensityTV"
        app:layout_constraintTop_toTopOf="@id/intensityTV"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="Pütürge, Malatya" />

    <TextView
        android:id="@+id/minutesPassedTV"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/earthquake_text_secondary"
        android:textSize="11sp"
        app:layout_constraintBottom_toBottomOf="@id/intensityTV"
        app:layout_constraintEnd_toStartOf="@id/metadataDivider"
        app:layout_constraintStart_toEndOf="@id/intensityTV"
        app:layout_constraintTop_toBottomOf="@id/countryTV"
        tools:text="10 m ago" />

    <View
        android:id="@+id/metadataDivider"
        android:layout_width="1dp"
        android:layout_height="38dp"
        android:layout_marginEnd="15dp"
        android:background="@color/earthquake_stroke"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/dateTimeTV"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/dateTimeTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/earthquake_text_secondary"
        android:textSize="11sp"
        app:layout_constraintBottom_toTopOf="@id/distanceTV"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/intensityTV"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="11 May · 13:06" />

    <TextView
        android:id="@+id/distanceTV"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:textColor="@color/earthquake_text_primary"
        android:textSize="11sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/intensityTV"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dateTimeTV"
        tools:text="126 km" />

</androidx.constraintlayout.widget.ConstraintLayout>
